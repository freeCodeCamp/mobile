pre-commit:
  commands:
    pretty:
      glob: "*.dart"
      run: flutter format {staged_files} && git add {staged_files}
    validate:
      run: bash -c 'cd mobile-app && flutter pub run ./git_hooks/commit_message.dart'
    linter:
      run: flutter analyze