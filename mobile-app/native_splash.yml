# flutter pub run flutter_native_splash:create --path=./native_splash.yml
flutter_native_splash:
  image: assets/icon/icon.png
  color: "#0a0a23"

  # Android
  android: true
  android_12:
    color: "#0a0a23"

  # iOS
  ios: true
  info_plist_files:
    - "ios/Runner/Info-Debug.plist"
    - "ios/Runner/Info-Release.plist"
    - "ios/Runner/Info-Profile.plist"
