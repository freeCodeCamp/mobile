flutter_native_splash:
  ios: true
  android: true
  image: assets/icon/icon.png
  color: "#0a0a23"
  info_plist_files:
    - "ios/Runner/Info-Debug.plist"
    - "ios/Runner/Info-Release.plist"
    - "ios/Runner/Info-Profile.plist"
  android_12:
    color: "#0a0a23"
# run this to apply any changes:
# dart run flutter_native_splash:create --path=./native_splash.yml
