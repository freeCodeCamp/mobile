/// Legacy placeholder.
///
/// This project previously used annotation-based code generation in this file.
/// Routing and dependency registration are now maintained in:
/// - `app.router.dart` (route names + argument objects)
/// - `app_router.dart` (MaterialApp.onGenerateRoute)
/// - `app.locator.dart` (GetIt registrations)
class AppSetup {
  const AppSetup._();
}
